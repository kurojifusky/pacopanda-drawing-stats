{
  "private": true,
  "name": "pacopanda-drawing-stats",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "prettier --check . && eslint --quiet .",
    "format": "prettier --write . && eslint --quiet --fix ."
  },
  "workspaces": [
    "client",
    "packages/**"
  ],
  "devDependencies": {
    "@pds/eslint-config-base": "*",
    "@turbo/gen": "latest",
    "eslint": "^8.57.0",
    "prettier": "^3.3.1",
    "prettier-plugin-svelte": "^3.2.4",
    "prettier-plugin-tailwindcss": "^0.5.11",
    "turbo": "latest"
  },
  "peerDependencies": {
    "eslint": ">= 8.5"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
